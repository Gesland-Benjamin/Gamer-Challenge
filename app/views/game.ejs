<%- include('partials/header') %>
<%- include('partials/aside_nav') %>

<main class="game-page-main" role="main">
  <section class="game-header" aria-label="Présentation du jeu">
    <div class="game-cover-wrapper">
      <img src="<%= game.picture %>" alt="<%= game.name %>" class="game-cover" />
    </div>

    <div class="game-info">
      <h2 id="game-name" class="game-name"><%= game.name %></h2>

      <p class="game-description">
        <%= game.description %>
      </p>

      <p class="game-date">
        <%= game.release_year.getFullYear() %>
      </p>

      <p class="game-meta" aria-hidden="false">
        <%= game.genre %>
      </p>
    </div>

    <% if (game.challenges && game.challenges.length) { %>
      <% game.challenges.forEach(function(challenge) { %>
        <div class="challenge">
          <h2>
            <a href="/challenges/<%= challenge.id %>">
              <%= challenge.name %>
            </a>
          </h2>
          <p>
            Votes: <span id="votes-<%= challenge.id %>"><%= challenge.dataValues.voteCount %></span>
          </p>

          <!-- Bouton Like -->
          <button class="like-btn" data-id="<%= challenge.id %>">J'aime</button>
        </div>
      <% }); %>
    <% } else { %>
      <p>Aucun challenge disponible.</p>
    <% } %>
    <% if (user.role === 'admin') { %>
  <form action="/games/<%= game.id %>/delete" method="POST">
    <button type="submit">Supprimer</button>
  </form>
<% } %>
    <a href='/challenges/<%= game.id %>/addChallenge' class="button">Ajouter un challenge</a>
    <a href='/' class="button">Accueil</a>
  </section>
</main>

<%- include('partials/footer') %>

<!-- Script pour gérer le Like -->
<script src="/js/like.js"></script>
